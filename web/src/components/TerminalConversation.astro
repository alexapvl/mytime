<div
  class="terminal terminal-conversation"
  data-terminal-conversation
  role="region"
  aria-label="Animated agent conversation where mytime CLI commands run behind the scenes"
>
  <div class="terminal-bar">
    <span class="terminal-dot"></span>
    <span class="terminal-dot"></span>
    <span class="terminal-dot"></span>
    <span class="terminal-title">agent · mytime</span>
    <button
      type="button"
      class="tc-replay"
      data-tc-replay
      aria-label="Play next scenario"
      disabled
    >
      replay
    </button>
  </div>
  <div class="terminal-body">
    <div class="tc-thread" data-tc-thread>
      <div class="tc-turn" data-tc-turn hidden></div>
    </div>
    <div class="tc-composer" data-tc-composer>
      <span class="tc-composer-prompt t-muted" aria-hidden="true">›</span>
      <div class="tc-composer-field" data-tc-composer-field>
        <span class="tc-composer-text" data-tc-composer-text></span><span
          class="tc-cursor"
          data-tc-cursor
          aria-hidden="true">|</span>
      </div>
    </div>
  </div>
</div>

<script>
  import "../scripts/terminal-conversation";
</script>

<style>
  .terminal-conversation .terminal-bar {
    gap: 0.5rem;
  }

  .terminal-conversation .terminal-title {
    flex: 1;
    min-width: 0;
  }

  .tc-replay {
    margin-left: auto;
    padding: 0.2rem 0.5rem;
    border: 1px solid var(--border);
    border-radius: 0.25rem;
    background: transparent;
    color: var(--text-muted);
    font-family: var(--font-mono);
    font-size: 0.625rem;
    letter-spacing: 0.04em;
    text-transform: lowercase;
    cursor: pointer;
    transition:
      color 0.15s ease,
      border-color 0.15s ease,
      background 0.15s ease;
  }

  .tc-replay:hover:not(:disabled) {
    color: var(--cyan);
    border-color: var(--border-bright);
    background: rgb(255 255 255 / 0.03);
  }

  .tc-replay:disabled {
    opacity: 0.45;
    cursor: default;
  }

  .tc-replay:focus-visible {
    outline: 2px solid var(--cyan);
    outline-offset: 1px;
  }

  .terminal-conversation {
    height: 18.5rem;
    display: flex;
    flex-direction: column;
  }

  .terminal-conversation .terminal-body {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    padding: 0;
    overflow: hidden;
  }

  .tc-composer {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    flex-shrink: 0;
    padding: 0.75rem 1rem;
    border-top: 1px solid var(--border);
    background: rgb(255 255 255 / 0.03);
    max-height: 3.25rem;
    overflow-y: auto;
  }

  .tc-composer-prompt {
    flex-shrink: 0;
    line-height: 1.45;
  }

  .tc-composer-field {
    flex: 1;
    min-height: 1.45em;
    line-height: 1.45;
    white-space: pre-wrap;
    word-break: break-word;
  }

  .tc-composer-text {
    transition: opacity 0.15s ease;
  }

  .tc-composer.is-sending .tc-composer-text {
    opacity: 0;
  }

  .tc-thread {
    flex: 1;
    min-height: 0;
    overflow-x: hidden;
    overflow-y: auto;
    padding: 0.875rem 1rem 0.5rem;
    scroll-behavior: smooth;
  }

  .tc-turn {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .tc-user-bubble {
    align-self: flex-end;
    max-width: 88%;
    padding: 0.5rem 0.75rem;
    border-radius: 0.625rem 0.625rem 0.2rem 0.625rem;
    background: rgb(93 212 232 / 0.16);
    border: 1px solid rgb(93 212 232 / 0.22);
  }

  .tc-user-text {
    margin: 0;
    font-size: 0.8125rem;
    line-height: 1.5;
    color: var(--text);
  }

  .tc-land {
    animation: tc-land 0.32s cubic-bezier(0.22, 1, 0.36, 1);
  }

  @keyframes tc-land {
    from {
      opacity: 0;
      transform: translateY(0.5rem);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .tc-tool {
    align-self: flex-start;
    max-width: 92%;
    padding: 0.5rem 0.625rem;
    border: 1px solid var(--border);
    border-radius: 0.375rem;
    background: rgb(0 0 0 / 0.25);
  }

  .tc-tool-cmd {
    font-size: 0.6875rem;
    color: var(--text-muted);
    margin-bottom: 0.375rem;
  }

  .tc-tool-status,
  .tc-tool-result {
    font-size: 0.6875rem;
    line-height: 1.45;
  }

  .tc-tool-result {
    color: var(--cyan-dim);
  }

  .tc-reply {
    align-self: flex-start;
    max-width: 92%;
  }

  .tc-reply-label {
    display: block;
    margin-bottom: 0.25rem;
    font-size: 0.625rem;
    font-weight: 600;
    letter-spacing: 0.03em;
  }

  .tc-reply-text {
    margin: 0;
    font-size: 0.8125rem;
    line-height: 1.55;
    color: var(--text-muted);
  }

  .tc-line-enter {
    animation: tc-line-in 0.2s ease-out;
  }

  @keyframes tc-line-in {
    from {
      opacity: 0;
      transform: translateY(0.15rem);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .tc-cursor {
    color: var(--cyan);
    animation: tc-blink 1s step-end infinite;
  }

  @keyframes tc-blink {
    50% {
      opacity: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .tc-cursor {
      animation: none;
    }

    .tc-land,
    .tc-line-enter {
      animation: none;
    }

    .tc-thread {
      scroll-behavior: auto;
    }
  }
</style>
